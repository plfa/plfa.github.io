[bumpver]
current_version = "v20.07"
version_pattern = "v0Y.0M[.PATCH]"
commit_message = "Bump version {old_version} -> {new_version}"
commit = true
tag = true
push = true

[bumpver.file_patterns]
"bumpver.toml" = ['current_version = "{version}"']
"plfa.cabal" = ['version:            {pep440_version}']
